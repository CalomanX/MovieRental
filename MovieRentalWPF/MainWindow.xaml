<Window x:Class="MovieRentalWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MovieRentalWPF"
        mc:Ignorable="d"
        Title="Movie Rental Management System" Height="600" Width="1000"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Padding="20,15" Background="Black">
            <TextBlock Text="Movie Rental Management System" 
                       FontSize="24" FontWeight="Bold" 
                       Foreground="White" HorizontalAlignment="Center"/>
        </Border>
        
        <!-- Main Content -->
        <TabControl Grid.Row="1" Margin="10" Background="White">
            <!-- Movies Tab -->
            <TabItem Header="Movies" FontSize="14" Padding="15,8">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Movies Header -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                        <TextBlock Text="Movie Management" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
                        <Button Name="RefreshMoviesButton" Content="Refresh" Margin="20,0,0,0" Padding="15,5" Click="RefreshMoviesButton_Click"/>
                    </StackPanel>
                    
                    <!-- Movies List -->
                    <DataGrid Grid.Row="1" Name="MoviesDataGrid" AutoGenerateColumns="False" 
                              CanUserAddRows="False" CanUserDeleteRows="False" 
                              GridLinesVisibility="Horizontal" HeadersVisibility="Column"
                              AlternatingRowBackground="#F8F9FA">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="80" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="*" IsReadOnly="True"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    
                    <!-- Add Movie Section -->
                    <Border Grid.Row="2" BorderBrush="#E9ECEF" BorderThickness="1" Padding="15" Margin="0,15,0,0">
                        <StackPanel>
                            <TextBlock Text="Add New Movie" FontWeight="Bold" Margin="0,0,0,10"/>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Title:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <TextBox Name="MovieTitleTextBox" Width="300" Padding="5" Margin="0,0,15,0"/>
                                <Button Name="AddMovieButton" Content="Add Movie" Padding="15,5" Click="AddMovieButton_Click"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </Grid>
            </TabItem>
            
            <!-- Customers Tab -->

            <!-- Rentals Tab -->
        </TabControl>
        
        <!-- Status Bar -->
        <StatusBar Grid.Row="2" Background="#34495E">
            <StatusBarItem>
                <TextBlock Name="StatusTextBlock" Text="Ready" Foreground="White"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
